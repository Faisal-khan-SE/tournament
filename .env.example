# Copy to .env and fill in. The SQLite file is fixed at prisma/dev.db.

# Organiser/scorer password. Leave unset to run the site open (private network
# only). When set, every write — scoring, editing, deleting — needs a sign-in
# at /login, or an "Authorization: Bearer <password>" header for API clients.
ADMIN_PASSWORD=
